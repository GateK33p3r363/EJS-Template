<header id="top-header">
    <span class="siteName">
        <a href="/" title="Return to home page">CSE Motors</a>
    </span>
    <?php
    // Check if the client is logged in
    $loggedIn = /* logic to determine if the client is logged in */;

    if ($loggedIn) {
        // Display the "Welcome Basic" link
        echo '<div id="welcome-link">';
        echo '<a href="/account-management" title="Click to manage your account">Welcome Basic</a>';
        echo '</div>';
        // Display the "Logout" link
        echo '<div id="tools">';
        echo '<a title="Click to log out" href="/account/logout">Logout</a>';
        echo '</div>';
    } else {
        // Display the "My Account" link
        echo '<div id="tools">';
        echo '<a title="Click to log in" href="/account/login">My Account</a>';
        echo '</div>';
    }
    ?>
</header>